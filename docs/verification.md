# Verification record

Verified locally on Windows, Python 3.13, on 8 September 2026.

Finding navigation passed three focused editor tests using real validation results: native click/click/double-click event order followed by fixing a loose screw, an unsupported finding without a part ID followed by Drop to floor, and Enter opening a grouped component's part controls without altering its document. Revalidation cleared both repaired findings. JavaScript syntax and the packaged release checks passed. Chrome additionally verified a native double-click switching to Properties for the floating box and focusing Drop to floor. The live server serves the updated UI; its API remains version 10. The wheel is 845,726 bytes with SHA-256 `c2e1dc248a261fbe863e6ce749c791705a023df2818b572ab6c99641ab6ec348`.

**Drop to floor** passed 11 new geometry/placement cases and 76 existing posing, grouping, preview-cache and server cases, with the existing Windows symlink skip. Seven focused editor checks passed, covering lowering/lifting, placement under Position, whole-person pose preservation, blocked world fixings, repeat-click no-ops and Undo/Redo, plus existing articulated movement flows. Geometry cases include rotated compounds, exact round-pipe support, scaled/offset imported meshes, connected rigid and articulated branches, ceiling supports, limited travel and hinged parts whose extents change during the move. JavaScript syntax and the packaged wheel checks also passed, including placement of a 200-link grouped chain outside the checkout.

Chrome verification placed a horizontal C42 tube from Z = -10 mm onto the plane at centre Z = 21.2 mm, and checked the button beneath Position and Undo/Redo. The running editor serves API version 10 with its session retained. `output/floor-live-check.json` independently verifies drops from Z = 500 mm and lifts from Z = -10 mm, both ending with the lowest point at Z = 0 to numerical precision. No saved design was changed. The rebuilt wheel is 844,895 bytes; SHA-256 `1546be92e90815f9d6b2d525f484a0406979519df1b178d9b7e728fc9ac88af1`.

The assembly-allowance and bounded Force update passed the full Python suite: **367 passed, 1 skipped**, in 492.60 seconds. The existing skip requires Windows Developer Mode for symbolic links. All **87 editor interaction/snapping checks** passed in 146.45 seconds. The 23 new Python cases cover an otherwise locked 1 mm gap, serialized per-socket allowance, temporary connector releases and retightening, bounded individual and simultaneous pipe resizes, rotated ceiling frames, travel and engagement rejection, actual resized-geometry collisions, grouped humans and animation, invalid settings, and rotation without accumulating clearance. UI cases cover the editable allowance, proposed screw/length changes, cancellation of a pending resize, corrected inputs, and one-step Undo/Redo.

Chrome verification used `examples/assembly-adjustments.pipe.yaml`. The ordinary preview accepted its 1.00 mm residual at the default 2 mm allowance. Enabling one pipe resize with a 2 mm maximum produced `left-arm: 401.00 → 400.00 mm`; Cancel preserved all 13 original joints and the 401 mm length. Connect created the fourteenth joint and changed the length in one edit. Undo/Redo restored the original and accepted source documents. Independent checking of `output/verified-fit-browser.json` found 13 parts, 14 joints, unchanged world anchors and prior lock states, no intersections, and a maximum joint gap below 0.000000001 mm.

The rebuilt wheel passed extraction and both bounded Force paths outside the checkout, alongside the existing triangle, human, chain and cached-preview release checks. Its SHA-256 is `874d85b5aa0a6d2f3cd42a40d8709adb08aacd5e26abcc802130901f24a07e3f`; it is 843,466 bytes. The running editor now serves API version 9, with its session token retained. `output/fit-allowance-live-check.json` records the default allowance and an exact Force resize through the running HTTP endpoint, without saving a design. Save unsaved work before refreshing an existing tab to load the new controls.

The constrained-preview update uses cached libraries, resolved documents and joint graphs, plus pose-only drag responses and exact preview acceptance. The full Python suite passed **344 tests**, with one existing Windows symbolic-link skip, in 586.63 seconds. All **82 editor interaction/snapping checks** passed, including cancellation during preview and commit, failed commits, latest-target coalescing and one-step Undo/Redo. Cache coverage includes changed locks, limits, world anchors, cut lengths, saved states, library overrides, file replacement/deletion, separate directories/tabs, concurrent targets, exact result acceptance and eviction fallback.

`python scripts/benchmark_preview.py --compare` measured these first frames after opening each scene. The new timings include local HTTP and serialization; the old comparison reproduces the former full-library/full-document pipeline without HTTP overhead. Targets change between frames, with the previous solution used as the next seed. These are local measurements, not timing guarantees for arbitrary mechanisms.

| Example | Former first frame | New first frame | Following frames, median | Release/save |
| --- | ---: | ---: | ---: | ---: |
| Hanging person's shin | 2430 ms | 82 ms | 55 ms | 625 ms |
| Hinged triangle fitting | 2173 ms | 16 ms | 18 ms | 170 ms |
| Loose sliding collar | 2090 ms | 10 ms | 11 ms | 101 ms |
| Posing a 200-link chain | 3373 ms | 218 ms | 211 ms | 1503 ms |

Raw results are in `output/preview-benchmark.json`. The hanging-leg response shrank from 48,184 bytes to 1,000 bytes. In Chrome, dragging a seated person's shin moved its hip and knee and retained all 18 anatomical joints. Undo/Redo restored both poses. Independent checking of `output/verified-preview-browser.json` found one grouped person and a maximum joint-position error of 0.000000014 mm. The rebuilt wheel passed extraction, cached preview and commit checks outside the source checkout; its hash is in `output/release-check.json`.

The running editor was updated to API version 8 with its existing session token retained. A live check verified the lightweight response, exact preview commit and legacy preview contract without saving a design. Existing browser tabs need a refresh to load the new frontend; save unsaved work first.

The chain-object update passed the full Python suite (**323 passed, 1 skipped**) and all **80 editor interaction/snapping checks**. A subsequent portable-chain regression and the affected chain/grouping tests passed (**38 cases**, bringing the Python total to **324 passing cases**). The skip requires Windows Developer Mode for symbolic links. Chain coverage includes whole-link rounding, library pitch, invalid lengths, 200-link layout and posing, repeated drags, rotation, retained shape on resize, saved coordinates, world anchors, end attachments, detach/reconnect, duplication, portable regrouping, physical flexibility and grouped simulation-frame capture. A 200-link chain has no internal layout solver variables in whole-object mode; its standard posing path avoids the dense mechanism solver.

Chrome checks changed a chain to 800 mm, released its example anchor, moved it 3000 mm, turned it 180 degrees and lifted its end link by 30 mm before holding the resulting shape as one object. `output/verified-chain-browser.json` retains one object, 40 links, 39 aligned joints and one layout group. Independent validation found no intersections and a maximum joint-position error below 0.000001 mm. The rebuilt wheel passed chain generation, 200-link posing and length editing outside the source checkout. Its hash is recorded in `output/release-check.json`. The live editor serves API version 7 and its chain length endpoint was verified without saving a design.

The hinged-connection fitting update passed **97 Python regression cases**, with one existing Windows symbolic-link skip, across the fitting, snapping, sliding, posing, grouping and server tests. All **75 editor interaction/snapping checks** passed across the full run and a focused rerun after updating an assertion for the improved failure message. New coverage includes threading three tee-ended legs on a square despite 15-degree snap settings, concurrent hinge/slide movement, rotated and unanchored frames, constrained travel, incompatible and occupied sockets, saved poses, motor/animation/drive rebasing, Force previews, cancellation and one-step Undo.

Chrome checks built the final two connections in **Examples → Hinged triangle**, including a Force search that adjusted the requested socket station. The captured `output/verified-triangle-browser.json` has 18 parts and 20 aligned connections, no intersections, unchanged square geometry and unchanged cut lengths. Independent checks found a maximum joint-position error below 0.000001 mm and confirmed that every pre-existing connection remained within its available motion. The updated wheel passed extraction and both triangle-fitting operations outside the source checkout; its hash is recorded in `output/release-check.json`. The live editor serves API version 6, with Force fitting and example discovery verified without saving a design.

The duplicate-menu update passed **20 duplication tests**, **17 grouping tests**, and **51 server/file-format tests** (88 passed, with one Windows symbolic-link skip). The full `npm run check` passed **72 editor interaction/snapping tests**. Coverage includes single generated limbs, immediate neighbours through loose joints, locked subassembly boundaries, whole grouped humans, saved coordinates with world anchors, motors/tracks/drives, mesh references after Save As, count validation, cancellation, keyboard navigation, and one-step batch Undo/Redo.

Chrome checks verified the dark-mode menu, three new copies with one Undo, and duplication of the complete workbench. `output/verified-duplicate-browser.json` contains 26 parts; every copied internal connection was checked for alignment, and original poses and world anchors were unchanged. The updated wheel passed independent extraction and single-limb/whole-human duplication checks. The duplicate endpoint was verified without saving a file.

The articulated-object and TC148 update passed the full Python suite (**264 passed, 1 skipped**) and six subsequently added regression cases (**270 passing cases total**). The skip requires Windows Developer Mode for symbolic links. All **66 editor interaction/snapping checks** passed across the full run and focused reruns after fixes. The new cases cover whole-person translation/flipping, limb posing without losing grouping, legacy regrouping, muscle controls, saved state, portable bundles, world anchors, attachment previews, undo/redo, and all five TC148 sizes including a paired arrangement.

In Chrome, a person was translated 3000 mm, turned 180°, posed by moving a forearm 30 mm, expanded and regrouped. The captured design in `output/verified-regroup-browser.json` retains one object and all 18 connected anatomical joints. The TC148 catalogue mesh loaded in the editor; its example also rendered through the CLI. The updated wheel passed extraction, mesh loading and regroup/move operations outside the checkout. Its current hash is recorded in `output/release-check.json`.

Earlier whole-product verification:

- `python -m pytest -q`: **120 passed**, 75.89 seconds.
- `node --check pipesim/web/app.js`: passed.
- Wheel built successfully with isolated setuptools dependencies.
- `scripts/verify_release.py`: offline JavaScript assets, schemas, libraries and solver modules present; catalogue command runs from the extracted wheel outside the source package.
- Browser: workbench loads; socket locking changes inferred body count; assembly search and export succeed; whole-human placement and posture controls work; simulation records 31 frames; Save As succeeds; browser error log is empty.
- Printable workbench book: **19 pages**, including **13 assembly steps**, layout, cutting plan and engineering figures. All images loaded. Page extents fit A4.
- MP4 export: H.264, 640 × 800, 31 frames for the sliding-collar example. Separate GT2 and seated-posture videos were also generated.

Local deliverables are in `output/workbench-build`, `output/wheels`, and `output/*.mp4`. The package check and hash are in `output/release-check.json`.

The cross-platform CI matrix is configured for Windows, Linux and macOS but has not been run on a remote CI service. Local numerical tests validate the implemented mathematical models, not the physical strength of purchased hardware or the accuracy of an individual human model. See [model limits](model-limits.md).
